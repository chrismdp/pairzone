
if [[ -d "${rvm_path:-$HOME/.rvm}/environments" \
  && -s "${rvm_path:-$HOME/.rvm}/environments/ruby-1.9.3@pairzone" ]] ; then
  \. "${rvm_path:-$HOME/.rvm}/environments/ruby-1.9.3@pairzone"
else
  rvm --create  "ruby-1.9.3@pairzone"
fi
